<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Task Details</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body { font-family: 'Inter', sans-serif; }
  </style>
  <link rel="shortcut icon" href="/images/icon.png" type="image/x-icon">
</head>
<body class="bg-gradient-to-br from-gray-800 to-gray-900 min-h-screen flex items-center justify-center">
  <div class="container mx-auto p-6">
    <div class="bg-slate-800 p-6 rounded-xl max-w-md w-full space-y-4">
      <h2 class="text-xl font-bold text-white">Task Details</h2>
      <p class="text-gray-200"><strong>Title:</strong> <%= task.title %></p>
      <p class="text-gray-200"><strong>Email:</strong> <%= task.email %></p>
      <p class="text-gray-200"><strong>Due Date:</strong> <%= new Date(task.dueDate).toLocaleString() %></p>
      <p class="text-gray-200"><strong>Details:</strong> <%= task.details %></p>
      <div class="flex justify-end space-x-2">
        <a href="/edit/<%= encodeURIComponent(task.title) %>" class="bg-blue-600 hover:bg-blue-700 px-6 py-3 rounded-lg font-semibold text-white">Edit</a>
        <a href="/" class="bg-slate-600 hover:bg-slate-700 px-6 py-3 rounded-lg font-semibold text-white">Back</a>
      </div>
    </div>
  </div>
</body>
</html>